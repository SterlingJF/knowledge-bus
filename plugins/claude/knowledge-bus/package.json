{
  "name": "@knowledge-bus/claude",
  "version": "0.5.0",
  "private": true,
  "description": "Knowledge Bus workflows for Claude Code",
  "license": "MIT",
  "files": [".claude-plugin/plugin.json", "skills/", "references/", "runtime/", "LICENSE"],
  "scripts": {
    "build": "uv run --locked --project ../../.. python ../../../tools/plugin/plugin.py build claude",
    "check": "uv run --locked --project ../../.. python ../../../tools/plugin/plugin.py check claude",
    "prepack": "node -e \"throw new Error('Pack the assembled artifact with just plugin-build claude, not this source adapter.')\""
  }
}
