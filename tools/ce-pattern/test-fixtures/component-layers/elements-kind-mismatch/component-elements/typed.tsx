import { Button, type ButtonProps } from "../component-core/button";

export function Typed(props: ButtonProps) {
  return <Button {...props} />;
}

export function TypedGhost(props: ButtonProps) {
  return <Button {...props} />;
}
