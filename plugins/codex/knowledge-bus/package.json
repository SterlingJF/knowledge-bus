{
  "name": "@knowledge-bus/codex",
  "version": "0.6.0",
  "private": true,
  "description": "Knowledge Bus workflows for Codex",
  "license": "MIT",
  "files": [".codex-plugin/plugin.json", "skills/", "references/", "runtime/", "LICENSE"],
  "scripts": {
    "build": "uv run --locked --project ../../.. python ../../../tools/plugin/plugin.py build codex",
    "check": "uv run --locked --project ../../.. python ../../../tools/plugin/plugin.py check codex",
    "prepack": "node -e \"throw new Error('Pack the assembled artifact with just plugin-build codex, not this source adapter.')\""
  }
}
